# Content admin API

Documentation for **only** `/api/v2/content-admin` (read blocks, update text, update image URLs).

Canonical copy: **`serverControl/src/api/contentAdmin/CONTENT_ADMIN_API.md`**

In `@cms-builder/core`: `cmsApi.getContentAdminBlocks`, `getContentAdminTree`, `postContentAdminText`, `postContentAdminImages`, `postContentAdminMedia` (all require `bearerToken` + project name).
